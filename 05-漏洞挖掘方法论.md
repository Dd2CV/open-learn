# 05 - 漏洞挖掘方法论

> 优先级：★★★ 实战核心
>
> **法律声明**：以下内容仅用于合法授权的安全测试。

## 一、标准化流程

```
信息收集 → 漏洞发现 → 漏洞验证 → 漏洞利用 → 报告输出
```

### 实战案例（电商支付漏洞）

1. **信息收集**：Fofa搜 `title=XX电商 测试环境`，找到 `test-pay.xx.com`
2. **漏洞探测**：抓包发现 `discount=0.9` 参数
3. **漏洞验证**：改成 `0.001`，订单从1000元变1元，支付成功
4. **漏洞利用**：录屏证明资金损失
5. **报告输出**：附抓包截图，建议后端校验折扣下限

---

## 二、信息收集（90%的漏洞藏在"隐藏资产"里）

### 核心逻辑
主站防护严密，测试环境、管理后台、子域名等隐藏资产才是漏洞高发区。

### 收集方法

| 维度 | 工具 | 实战步骤 |
|---|---|---|
| 子域名 | OneForAll、Fofa | 跑主域名→导出子域名→标注test/admin |
| 端口服务 | Nmap、Goby | 扫子域名IP→标高危端口→记录非标准端口 |
| 技术栈 | Wappalyzer、潮汐库 | 识别CMS/中间件→查版本对应已知漏洞 |
| WAF探测 | Nuclei、Burp | 发恶意请求→看是否有WAF拦截 |

### 信息收集要点
- 不要只依赖一个工具，OneForAll 跑不全要配合 Fofa/ZoomEye
- 重点关注测试环境（`test.xx.com`），防护极弱甚至无登录
- 记录异常资产（如IP在国外的未备案服务器）

---

## 三、漏洞探测（自动化+手动结合）

### 自动化扫描
```bash
# Xray被动扫描（配合浏览器代理）
xray webscan --listen 127.0.0.1:7777 --html-output result.html

# Nuclei模板扫描
nuclei -u https://target.com -t cves/

# 目录扫描
dirsearch -u https://target.com -e php,asp,jsp,html,js
```

### 手动测试重点
- **登录框**：SQL注入、用户名枚举、密码爆破、Session固定
- **文件上传**：绕过类型检查、路径穿越
- **参数传递**：IDOR（越权）、参数篡改
- **业务逻辑**：支付金额篡改、验证码绕过、重放攻击

---

## 四、常见漏洞类型与挖掘思路

### OWASP Top 10 核心漏洞

| 漏洞 | 挖掘思路 | 关键工具 |
|---|---|---|
| **SQL注入** | 输入点加 `'` `"` 看报错→联合查询/盲注 | SQLmap、Burp Suite |
| **XSS** | 输入框/参数注入 `<script>` →看是否执行 | Burp Suite、XSStrike |
| **文件上传** | 修改Content-Type→双扩展名→00截断 | Burp Suite |
| **SSRF** | URL参数替换为内网IP `http://127.0.0.1` | Burp Suite、SSRFmap |
| **反序列化** | 识别序列化数据特征→构造恶意链 | ysoserial、PHPGGC |
| **越权（IDOR）** | 修改用户ID/参数→看能否访问他人数据 | Burp Suite |
| **CSRF** | 构造跨站请求→看是否有Token校验 | Burp Suite |
| **命令注入** | 参数拼接 `;` `|` `&&` →看是否执行命令 | Burp Suite |
| **文件包含** | 参数修改为 `../../../etc/passwd` | Burp Suite |
| **XXE** | XML输入中注入外部实体声明 | XXExploiter |

### SQL注入挖掘详解

```
1. 找输入点（URL参数、POST表单、Cookie、HTTP头）
2. 测试注入点（加单引号看报错）
3. 判断注入类型（联合查询/盲注/时间盲注）
4. 获取数据库信息（版本、表名、列名）
5. 提取数据
```

### XSS 挖掘详解

```
1. 找输出点（页面回显、DOM操作、HTML属性）
2. 测试Payload（<script>alert(1)</script>）
3. 判断类型（反射型/存储型/DOM型）
4. 绕过过滤（编码、大小写、标签变形）
5. 构造利用（窃取Cookie、键盘记录、钓鱼）
```

---

## 五、漏洞验证与利用

### 漏洞验证原则
- 构造最小化PoC，证明漏洞存在即可
- 不破坏数据、不影响业务正常运行
- 截图/录屏保留证据

### 漏洞利用链设计
- 单个漏洞可能危害有限，组合利用效果倍增
- 常见组合：信息泄露→弱口令→文件上传→WebShell→提权→内网
- SSRF + Redis未授权 → RCE
- 文件包含 + 日志注入 → RCE
- 反序列化 + 命令执行 → GetShell

---

## 六、漏洞报告撰写

### 报告结构
```
1. 漏洞标题（简洁明了）
2. 漏洞等级（高/中/低，参考CVSS）
3. 漏洞描述（什么漏洞、在哪里）
4. 复现步骤（截图+说明，可复现）
5. 漏洞危害（可能造成的影响）
6. 修复建议（具体可操作）
7. 参考资料（CVE编号、相关链接）
```

### 修复建议示例
```
漏洞：SQL注入
位置：/login.php 的 username 参数
修复：使用预编译语句（PreparedStatement），
     禁止直接拼接SQL，添加输入长度和类型校验。
```

---

## 七、WAF绕过技巧

| 绕过方式 | 示例 |
|---|---|
| 大小写混合 | `ScRiPt` |
| 编码绕过 | URL编码、双重编码、Unicode编码 |
| 注释绕过 | `UN/**/ION SEL/**/ECT` |
| 空格替代 | `/**/`、`%09`、`%0a`、`()` |
| 等价函数 | `mid()` 替代 `substring()` |
| 分块传输 | Transfer-Encoding: chunked |
| HTTP参数污染 | `id=1&id=UNION SELECT` |

---

## 八、推荐工具链

### 信息收集
- Fofa / ZoomEye / Shodan — 网络空间搜索
- OneForAll / Sublist3r — 子域名枚举
- Wappalyzer — 技术栈识别
- Nmap / masscan — 端口扫描

### 漏洞扫描
- Xray — 被动扫描
- Nuclei — 模板化扫描
- AWVS / Nessus — 综合扫描

### 手动测试
- Burp Suite — 抓包/重放/爆破（核心工具）
- SQLmap — SQL注入自动化
- Dirsearch / Gobuster — 目录爆破

### 内网工具
- fscan — 内网综合扫描
- Mimikatz — 凭据提取
- Impacket — 横向移动工具套件
- BloodHound — AD攻击路径分析
