# 06 - 代码审计与AI漏洞挖掘

> 优先级：★☆☆ 高阶

## 一、代码审计基础

### 定义
代码审计是基于安全编码规范与攻击原理，对程序代码的执行流程、数据处理逻辑、权限控制机制进行系统性分析，发现潜在安全漏洞的过程。

### 不同身份的审计目标

| 身份 | 核心目标 | 应用场景 |
|---|---|---|
| 渗透测试工程师 | 挖掘可直接利用的高危漏洞 | 授权审计客户代码，发现注入、上传等漏洞 |
| 红队高级工程师 | 聚焦可突破防御的0day漏洞 | 审计靶标自研系统，构造专属Payload |
| 蓝队高级工程师 | 修复漏洞+恶意代码溯源 | 定期审计业务代码，应急响应时审计webshell |
| 网络攻防工程师 | 研究漏洞根源，设计防御体系 | 分析新型漏洞触发条件，制定审计规则 |

---

## 二、漏洞的代码根源

### 根源1：输入输出控制失效（最核心）
程序未对用户可控输入进行有效过滤/验证/转义：

```java
// SQL注入 - 直接拼接SQL语句
String sql = "SELECT * FROM user WHERE id = '" + request.getParameter("id") + "'";
// 输入 id=1' OR 1=1 -- 时，拼接为恶意SQL
```

**审计逻辑**：跟踪用户输入（`request.getParameter()`）的流向，看是否直接进入数据库查询、命令执行等危险操作。

### 根源2：代码执行流程失控
控制流存在缺陷，可绕过权限校验：

```php
// 垂直越权 - 仅前端判断权限
if ($_POST['role'] == 'admin') {
    // 攻击者可伪造 role=admin 提交
    echo "管理员后台";
}
```

**审计逻辑**：分析权限判断是否依赖用户可控参数，且未与后端会话/数据库真实权限比对。

### 根源3：违反安全编码规范
使用危险API或不安全配置：

```java
// 反序列化漏洞 - 读取不可信数据
ObjectInputStream ois = new ObjectInputStream(request.getInputStream());
Object obj = ois.readObject();
// 攻击者发送恶意序列化对象，触发代码执行
```

**审计逻辑**：搜索代码中危险API的使用，检查是否有输入校验。

---

## 三、代码审计流程

```
1. 入口点识别 → 2. 数据流追踪 → 3. 危险函数定位 → 4. 动态验证 → 5. 报告输出
```

### 步骤1：入口点识别
- Web路由（URL映射、Controller）
- API接口
- 文件上传点
- 反序列化入口
- 命令执行调用

### 步骤2：数据流追踪
跟踪用户输入从入口点到危险操作的完整路径：
```
用户输入 → 参数接收 → 数据处理 → 存储/执行
```

### 步骤3：危险函数定位

| 语言 | 危险函数 | 漏洞类型 |
|---|---|---|
| Java | `Runtime.exec()`, `ProcessBuilder` | 命令注入 |
| Java | `ObjectInputStream.readObject()` | 反序列化 |
| PHP | `eval()`, `assert()`, `system()` | 代码执行 |
| PHP | `include()`, `require()` | 文件包含 |
| Python | `eval()`, `exec()`, `os.system()` | 代码执行 |
| Python | `pickle.loads()` | 反序列化 |

### 步骤4：动态验证
- 构造Payload
- 在测试环境验证
- 排除误报
- 确认可利用性

---

## 四、AI驱动的漏洞挖掘新范式

### 行业现状
截至2026年5月，Anthropic安全团队已披露 **1,596个漏洞**，但仅 **97个被修复**。

**核心洞察：发现已可轻松并行化，瓶颈已转移到验证、分类和修复。**

### Anthropic 六步闭环

```
1. 威胁建模 → 2. 沙箱 → 3. 发现 → 4. 验证 → 5. 分类 → 6. 修复
```

#### 第1步：威胁建模 — 定义什么算漏洞
- 误报最常见原因：模型缺乏对信任边界的理解
- 从代码、文档、Git历史、历史CVE中引导生成威胁模型
- 在代码仓库放置 `THREAT_MODEL.md`，扫描前可读取

#### 第2步：沙箱 — 安全运行代理并验证可利用性
- **保护系统**：匹配隔离级别与威胁模型
- **证明可利用性**：构建可编译、运行测试、引爆PoC的沙箱
- 固定一切：镜像标签、commit SHA、依赖、构建命令

#### 第3步：发现 — 丰富上下文、简短提示
- 前沿模型受益于**越来越简单的提示**
- 过于规范的提示反而降低发现效果
- 长清单会减少模型的创造性

#### 第4步：验证 — 独立确认
- 静态扫描只能假设路径是否可达
- 动态沙箱验证显著降低不可利用的误报
- 给模型测试床、活系统、运行PoC是最大效能杠杆

#### 第5步：分类 — 去重、定级、排序
- 去重相似发现
- 按严重程度定级
- 排序修复优先级

#### 第6步：修复 — 应用补丁
- 应用补丁
- 确认漏洞失效
- 搜索类似变体

### 多智能体协同漏洞挖掘

将漏洞挖掘全流程拆解为多个智能体角色：

```
全局侦察收集 → 双线审计分析 → 攻击路径验证
     │              │              │
     ├── 架构梳理    ├── 静态分析    ├── PoC构造
     ├── 入口识别    ├── 动态分析    ├── 可利用性确认
     └── 攻击面界定  └── 漏洞初筛    └── 报告生成
```

**核心思路**：为大模型嵌入安全专家的标准化工作流框架，让审计从"AI无序盲审"进化到"多智能体编排协同作战"。

---

## 五、安全编码规范

### 输入验证
```java
// 错误 - 直接拼接
String sql = "SELECT * FROM users WHERE id=" + userInput;

// 正确 - 预编译
PreparedStatement stmt = conn.prepareStatement("SELECT * FROM users WHERE id=?");
stmt.setInt(1, Integer.parseInt(userInput));
```

### 输出编码
```java
// 防止XSS - HTML编码输出
String safeOutput = HtmlUtils.htmlEscape(userInput);
```

### 权限校验
```java
// 错误 - 依赖前端参数
if (request.getParameter("role").equals("admin")) { ... }

// 正确 - 后端会话校验
User user = (User) session.getAttribute("user");
if (user != null && user.isAdmin()) { ... }
```

### 文件上传安全
```java
// 校验文件类型（白名单）
String[] allowedTypes = {"image/jpeg", "image/png", "image/gif"};
// 重命名文件
String safeName = UUID.randomUUID().toString() + extension;
// 存储到非Web目录
```
